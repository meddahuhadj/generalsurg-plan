# Dépendances de test uniquement — ne pas installer en production.
# pip install -r requirements.txt -r requirements-dev.txt --break-system-packages
pytest>=8.0.0
pytest-asyncio>=0.24.0
hl7>=0.4.5
# Tests E2E navigateur (suite tests/e2e) — après pip install :
#   python -m playwright install chromium
playwright>=1.40.0
